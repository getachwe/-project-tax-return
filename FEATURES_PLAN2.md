### FEATURES PLAN 2 – רעיונות להמשך (ערך גבוה → מאמץ נמוך תחילה)

## 1) חוויית משתמש וקליטת נתונים

1. מסך Onboarding קצר (3 צעדים) המסביר מה נדרש ומה מקבלים בסוף
2. גרירת קבצים משופרת: תמיכה ב־PDF/תמונה מרובים וצירוף גרירה לכל העמוד
3. מילוי חכם: הצעות אוטומטיות לפי טקסט שזוהה (auto-fill) והשלמה לפי היסטוריה
4. אימותים בזמן אמת (schema validation) עם הסברים קצרים מתחת לשדה
5. סטטוס זיהוי בזמן OCR (progress + הערכת זמן)
6. דף “מה חדש” עם שינויים אחרונים ושיפורים

## 2) OCR ויבוא מסמכים

1. תמיכה ב־106 בפורמטים שונים (טמפלטים) עם זיהוי תבנית אוטומטי
2. OCR משולב: Tesseract + ספק חיצוני אופציונלי (למשל Google Vision) כ־fallback
3. פרה־עיבוד מתקדם: deskew, remove noise, adaptive threshold, upscale
4. זיהוי שדות לפי סמנטיקה (מילות מפתח + מרחק קוסינוס בין בלוקים)
5. הדגשת אזורים שנקראו במסמך (overlay) להצלבה ידנית מהירה
6. יבוא CSV/Excel של תלושים רבים ומיפוי עמודות דינמי לשדות מערכת

## 3) חישובים ותובנות

1. סימולציות “מה אם”: שינוי פרמטרים מרכזיים והשוואת תרחישים בטבלה/גרף
2. בדיקות היגיון מתקדמות (תאריכים, טווחים, חפיפה בין תקופות עבודה)
3. ניתוח רגישות: איזה שדה הכי משפיע על תוצאת ההחזר
4. טבלאות מס לפי שנה עם קונפיג קל לעדכון (versioned)
5. תמיכה בהקלות אזור עדיפות/יישובי פריפריה (טבלת יישובים)
6. אוטומציית השלמה לשדות חסרים (heuristics) עם שקיפות למה הושלם

## 4) דוחות וייצוא

1. תבנית PDF RTL משודרגת: דוחות “מלא” ו“תקציר מנהלים”
2. גרפים ותצוגות בעמודי הדוח (טור/קו/עוגה) עבור הכנסות/ניכויים
3. ייצוא CSV/Excel של קלט ותוצאות
4. לינק שיתוף מאובטח לקריאה בלבד (tokenized) עם expiry
5. התאמה אישית של כותרת/לוגו/צבעים לפי משתמש/ארגון

## 5) ניהול משתמשים ונתונים

1. תפריט ראשי עם סטטוס התחברות, פרופיל, היסטוריית דוחות והתנתקות
2. פרופיל מורחב: טלפון, כתובת, העדפות שפה ותבנית PDF מועדפת
3. הרשאות: משתמש, רו"ח, אדמין (צפייה/עריכה/שיתוף)
4. שיתוף תיק חישוב בין משתמשים מסוימים (כתובות מייל) עם audit trail
5. Self‑Service Data: הורדה/מחיקה מלאה (GDPR) + יצוא כל הדאטה בלחיצה

## 6) Backend ותשתיות

1. Rate limiting, size limits, CORS מוקפד
2. תור עיבוד לקבצים גדולים (BullMQ/Redis) עם תצוגת סטטוס
3. אחסון קבצים זמני ב־S3/GCS וניקוי מתוזמן
4. גרסאות API וחלוקת מודולים (כבר בוצע שלב ראשון) + בדיקות אינטגרציה
5. לוגים מובְנים (Winston) ורמות לוג, tracing בסיסי (OpenTelemetry) בהמשך

## 7) ניטור, איכות ואוטומציה

1. טסטי יחידה לחישובי מס (Jest) + Snapshot לדוחות PDF
2. טסטי אינטגרציה ל־API (Supertest) ו־E2E למסע משתמש (Playwright)
3. CI/CD (GitHub Actions): לינט, טסטים, build ודיפלוי
4. Sentry לדיווח שגיאות פרונט/בקנד

## 8) אינטגרציות

1. SMTP אמיתי (כבר מוכן) + תבניות דוא"ל יפות לשליחת דוחות
2. תשלומים (Stripe) לרכישת דוחות/ייעוץ
3. Webhooks חיצוניים ל־CRM/ERP של רו"ח

## 9) בינלאומיות

1. i18n מלא (עברית/אנגלית) + פורמטי מספר/תאריך לפי שפה
2. טקסטי עזרה דינמיים (tooltip/help) לפי הקשר

## 10) ביצועים וחוויית ריצה

1. Code‑splitting, lazy‑loading, caching תוצאות חישוב
2. אופטימיזציית תמונות ו־gzip/brotli בפרודקשן

---

עדיפות מוצעת (ספרינט קצר, 1–2 שבועות):

- UX: סטטוס התחברות בתפריט + מסך Onboarding (1.1, 5.1)
- OCR: שיפור פרה־עיבוד ודיווח סטטוס (2.3, 1.5)
- דוחות: תבנית “תקציר מנהלים” (4.1–4.2)
- איכות: טסטי יחידה לחישובים + CI בסיסי (7.1, 7.3)
