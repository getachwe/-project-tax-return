# api-legacy

עותק/שארית של מימוש API ישן. **הפריסה הנוכחית משתמשת ב־`backend/` + `server.js`.**

תיקייה זו מוזנת ב־`.vercelignore` ואינה חלק מבילד הייצור. ניתן למחוק או לארכב רק אחרי אימות שאין ייבוא מפה בשום מקום.
