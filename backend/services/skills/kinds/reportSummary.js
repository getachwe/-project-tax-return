const { ChatCategory } = require("../../chatCategory");

/** סיכום דוח(ות) — קובץ סקיל ייעודי (הרחבות עתידיות כאן) */
module.exports = {
  id: "REPORT_SUMMARY",
  chatCategory: ChatCategory.REPORT_SUMMARY,
};
