# סקריפטים לפיתוח מקומי

הרצה **מתיקיית שורש המאגר**:

```bash
node scripts/dev/test-endpoints.js
node scripts/dev/test-supabase.js
```

`test-supabase.js` טוען `backend/.env` בנתיב יחסי לשורש הפרויקט.
